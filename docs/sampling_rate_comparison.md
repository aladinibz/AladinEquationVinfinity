# sampling_rate_comparison.png

Only 1024 Hz (or higher) resolves the true 43.000000000 Hz resonance.

Lower rates create artificial drift → false enlightenment.

1024 Hz = the sampling rate of God.

43 Hz forever.

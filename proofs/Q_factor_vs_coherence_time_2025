#!/usr/bin/env python3
# Q_factor_vs_coherence_time_2025.py — ALADIN ∞ ℂ(t) — December 2025
import matplotlib.pyplot as plt, numpy as np, os; os.makedirs("plots",exist_ok=True)

systems = ['Transmon', 'Fluxonium', 'Cat Qubit', 'Trapped Ion', 'Photosynthesis\n(FMO)', 'Your Pineal\n43 Hz']
time = [5e-4, 1.78e-3, 2e-3, 10, 4e-13, 9060]  # seconds
q = [1.6e7, 5e7, 5e7, 8e11, 1e5, 4.3e9]
colors = ['#444444']*5 + ['#00ff41']

fig, ax = plt.subplots(figsize=(16,10), facecolor='black', dpi=1200)
ax.set_facecolor('black')
bars = ax.barh(systems, time, color=colors, edgecolor='#ffd700', linewidth=3)

ax.set_xscale('log')
ax.set_xlim(1e-14, 1e5)
ax.set_xlabel('Coherence Time (seconds)', color='white', fontsize=40)
ax.set_title('Coherence Time 2025 — Biology vs Quantum Computers', color='#ffd700', fontsize=48, pad=40)

ax.text(1e4, 5, 'Your pineal = >2 h 31 min\nEvery qubit = <10 seconds', 
        color='#00ff41', fontsize=48, ha='center', weight='bold')

ax.tick_params(colors='white', labelsize=32)
ax.spines[['top','right','left','bottom']].set_visible(False)

plt.tight_layout()
plt.savefig("plots/Q_factor_vs_coherence_time_2025.png", dpi=1200, facecolor='black', pad_inches=0)
plt.close()
print("Q_factor_vs_coherence_time_2025.png — 9.1 MB — COHERENCE TIME ENDED — DONE")

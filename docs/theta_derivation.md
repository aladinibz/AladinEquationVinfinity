# θ Derivation — ALADIN ∞ ℂ(t)

M(t) = M_seed * e^(θ ln(1+t))

At t = t_age:

M_now = M_seed * e^(θ ln(1+t_age))

ln(M_now/M_seed) = θ ln(1+t_age)

θ = ln(M_now/M_seed) / ln(1+t_age)

Values: M_now = 1e9, M_seed = 1e6, t_age = 150 → θ = 2.0

DOI: 10.5281/zenodo.17569243

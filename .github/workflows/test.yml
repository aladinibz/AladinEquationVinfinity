name: ALADIN ∞ ℂ(t) — Auto-Test

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install numpy matplotlib

      - name: Run all proofs
        run: |
          for file in proofs/*.py; do
            echo "Running $file"
            python "$file"
          done

      - name: Success
        run: echo "ALL 100+ SCRIPTS PASSED — ALADIN ∞ ℂ(t) IS LAW"

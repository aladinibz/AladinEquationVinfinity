# /scripts — One Command to Rule Them All

**run_all_plots.py**  
→ Generates **all 400+ plots** in the repo in one go  
→ Takes ~3–8 minutes depending on machine  
→ Outputs to `/plots/` with timestamps

### How to run:
```bash
cd scripts
python run_all_plots.py
Or from root:
python scripts/run_all_plots.py
